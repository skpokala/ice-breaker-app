{
  "name": "team-ice-breaker-app",
  "version": "1.0.8",
  "description": "A team ice breaker questions app with admin controls",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "version:patch": "node scripts/increment-version.js patch",
    "version:minor": "node scripts/increment-version.js minor",
    "version:major": "node scripts/increment-version.js major",
    "version:check": "node -e \"console.log('Current version:', require('./package.json').version)\"",
    "release-notes": "node scripts/generate-release-notes.js $(node -p require('./package.json').version)",
    "docker:dev": "./scripts/deploy.sh dev",
    "docker:prod": "./scripts/deploy.sh prod",
    "docker:build": "./scripts/deploy.sh build",
    "docker:stop": "./scripts/deploy.sh stop",
    "docker:health": "./scripts/deploy.sh health",
    "deploy:patch": "npm run version:patch && git add . && git commit -m \"feat: release version $(node -p require('./package.json').version)\" && git push",
    "deploy:minor": "npm run version:minor && git add . && git commit -m \"feat: minor release version $(node -p require('./package.json').version)\" && git push",
    "deploy:major": "npm run version:major && git add . && git commit -m \"feat: major release version $(node -p require('./package.json').version)\" && git push"
  },
  "keywords": [
    "ice-breaker",
    "team",
    "questions",
    "admin"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
